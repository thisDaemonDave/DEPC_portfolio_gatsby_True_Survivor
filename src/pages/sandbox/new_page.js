import React from 'react';
import Layout from '../../components/layout';

export default () => (
	<Layout>
		<h1>Heading for the sandbox NEW_PAGE</h1>
		<p>This is some test copy that needs to be some lorem ipsum</p>
	</Layout>
);
